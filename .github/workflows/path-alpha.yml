name: Path alpha

on:
  push:
    branches-ignore:
      - push-event-log
      - path-event-log-*
    paths:
      - alpha/**

permissions:
  contents: write

concurrency:
  group: path-event-log-alpha
  cancel-in-progress: false

jobs:
  record:
    name: Record alpha path match
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Append path workflow record
        env:
          GH_TOKEN: ${{ github.token }}
          PATH_WORKFLOW_LABEL: alpha
        run: .github/scripts/append-path-workflow-log.sh
