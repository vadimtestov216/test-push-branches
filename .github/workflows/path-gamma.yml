name: Path gamma

on:
  push:
    branches-ignore:
      - push-event-log
    paths:
      - gamma/**

permissions:
  contents: write

concurrency:
  group: push-event-log
  cancel-in-progress: false

jobs:
  record:
    name: Record gamma path match
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Append path workflow record
        env:
          GH_TOKEN: ${{ github.token }}
          PATH_WORKFLOW_LABEL: gamma
        run: .github/scripts/append-path-workflow-log.sh
